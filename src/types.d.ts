export type FilePattern = {
  projectType:string
  projectName: string
  sourceFolderName: string
  targetDir: string
  selectedServer: string
  selectedOrm:string
  selectedAuth: string,
  swaggerOption: boolean
}